# MongoDB connection string
MONGO_URL=mongodb://localhost:27017/promptcraft_db

# Anthropic API Key
ANTHROPIC_API_KEY=your_anthropic_api_key_here

# Optional: Set log level (DEBUG, INFO, WARNING, ERROR, CRITICAL)
LOG_LEVEL=INFO

# --- Auth JWT Settings --- M
# Generate a strong secret key (e.g., using `openssl rand -hex 32`)
SECRET_KEY=your_32_byte_hex_secret_key_here
ALGORITHM=HS256 # Signing algorithm
ACCESS_TOKEN_EXPIRE_MINUTES=30 # Token validity period
# --- End Auth ---