fastapi
uvicorn[standard]
motor
anthropic
pydantic[email]
pydantic-settings
python-dotenv
requests

# --- Auth Dependencies --- M
passlib[bcrypt]
python-multipart
python-jose[cryptography]
# --- End Auth --- M

# For optional CSV/Excel upload later (backend processing if chosen)
pandas
openpyxl

# xlsx was incorrectly added here; it's a frontend JS library, not a Python package. 